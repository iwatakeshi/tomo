/* eslint-disable no-param-reassign */
export default (() => {
  const Options = {};
  Options.Scanner = {
    ignore: {
      whitespace: true,
    },
    override: {
      isWhitespace: undefined,
    },
  };
  return Options;
})();
/* eslint-enable no-param-reassign */
